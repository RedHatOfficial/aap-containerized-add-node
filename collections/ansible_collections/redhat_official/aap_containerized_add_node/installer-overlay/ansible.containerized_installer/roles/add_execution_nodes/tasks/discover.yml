---
# Scaffold: implement discovery (see redhat_official.aap_containerized_add_node.discover_new_nodes).
#
# Intended behavior:
# 1. On automationcontroller[0], podman exec … awx-manage list_instances
# 2. Parse hostnames that have a heartbeat (ready)
# 3. Diff groups[add_execution_nodes_inventory_group] vs ready set
# 4. add_host each missing/incomplete host into add_execution_nodes_target_group
# 5. Assert receptor_peers is a real list (INI: receptor_peers='["peer.example.com"]')

- name: Additive discovery is not implemented yet
  ansible.builtin.fail:
    msg: >-
      installer-overlay scaffold: implement roles/add_execution_nodes/tasks/discover.yml
      (reference: redhat_official.aap_containerized_add_node roles/discover_new_nodes + list_instances).
      Target group should be '{{ add_execution_nodes_target_group }}'.
