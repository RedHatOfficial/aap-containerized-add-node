[defaults]
# Install this collection before running playbooks:
#   ansible-galaxy collection install -r requirements.yml
#   ansible-galaxy collection install --force --no-deps .
roles_path = ./roles
