[defaults]
stdout_callback = yaml
host_key_checking = false
forks = 10

[ssh_connection]
control_path = %(directory)s/%%h-%%r
ssh_args = -o ServerAliveInterval=30 -o ControlMaster=no -o ControlPersist=60s
retries = 5
pipelining = true
