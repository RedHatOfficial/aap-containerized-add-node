---
bugfixes:
  - >-
    preflight - Honor ``aap_add_node_controller_target`` for EN TCP/DNS connectivity
    checks to the controller listener port.
minor_changes:
  - >-
    Remove stale ``collections/`` duplicate tree references from install docs;
    document ``ansible-galaxy collection install`` and root ``playbooks/`` paths.
  - >-
    Document ``aap_add_node_controller_target`` for HA controller selection in
    README and CONVENTIONS.
  - Add ``generate_bundle`` role ``meta/argument_specs.yml`` and ``README.md``.
