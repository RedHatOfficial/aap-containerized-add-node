---
removed_features:
  - >-
    Drop unused ``aap_add_node_peers_from_control_nodes`` default (peering is
    inventory ``receptor_peers``; inbound dial uses
    ``aap_add_node_enable_controller_peer``).
minor_changes:
  - >-
    Clarify ``examples/add_node.secrets.yml`` so defaults and real opt-in flags
    are obvious for new users.
